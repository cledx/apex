<% content_for :title, "#{@item.name}" %>
<% if current_user.role == "manager" || current_user.role == "admin" %>
    <%= render "show_content", manager_view: true %>
<% else %>
    <%= render "show_content", manager_view: false %>
<% end %>