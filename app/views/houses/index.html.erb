<% content_for :title, "Upcoming Sales" %>

<div class="container my-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <span class="badge rounded-pill bg-light text-dark px-3 py-2 border">
      Upcoming Sales
    </span>

    <% if user_signed_in? && current_user.manager? %>
      <%= link_to "+ Add Sale", new_manager_house_path, class: "btn btn-primary btn-sm" %>
    <% end %>
  </div>

  <% if @houses.empty? %>
    <div class="my-5">
      <div class="p-5 p-md-5 bg-white bg-opacity-75 rounded-4 shadow-lg d-flex flex-column align-items-center justify-content-center text-center" style="backdrop-filter: blur(6px);">
        <h2 class="h4 mb-3">No Sales Right Now</h2>
        <p class="mb-4">
          but check out some of our old ones!
        </p>
        <%= link_to "Old Sales", old_sales_path, class: "btn btn-primary btn-lg" %>
      </div>
    </div>
  <% else %>
    <%= render "houses", houses: @houses %>
  <% end %>
</div>